@model IEnumerable<Major>

<ul class="nav">
    @foreach(var m in Model)
    {

        <!-- Sử dụng đồng bộ AJAX -->
        <!--<li class="nav-item">
            <a asp-controller="Learner" asp-action="Index"
                asp-route-mid="@m.MajorID" class="nav-link"> @m.MajorName</a>
        </li>-->

        <!-- Không sử dụng đồng bộ AJAX -->
        <li class="nav-item" id="@m.MajorID">
            <a class ="nav-link"> @m.MajorName </a>
        </li>

    }
</ul>